interface:
  display_name: "Commit Push PR"
  short_description: "Commit changes, push branch, and open PR"
  default_prompt: "Use $commit-push-pr to commit the current changes, push the branch, and open a PR."
policy:
  allow_implicit_invocation: true
