interface:
  display_name: "Agree and Execute"
  short_description: "Continue after brief user approval"
  default_prompt: "Use $agree-and-execute to continue the already approved next steps."
policy:
  allow_implicit_invocation: true
