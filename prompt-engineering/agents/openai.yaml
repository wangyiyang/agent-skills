interface:
  display_name: "Prompt Engineering"
  short_description: "Design and refine prompts, skills, hooks, and agent instructions"
  default_prompt: "Use $prompt-engineering to design or improve this prompt, skill, hook, or agent instruction."
policy:
  allow_implicit_invocation: true
