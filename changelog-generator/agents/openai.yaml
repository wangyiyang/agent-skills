interface:
  display_name: "Changelog Generator"
  short_description: "Create user-facing changelogs from git history"
  default_prompt: "Use $changelog-generator to create a user-facing changelog or release notes from git commits."
policy:
  allow_implicit_invocation: true
